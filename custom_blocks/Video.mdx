---
name: Video
---
export function Video({ src }) {
  return (
    <div style={{ 
      width: '100%', 
      margin: '2rem auto',
      borderRadius: '8px',
      overflow: 'hidden'
    }}>
      <video
        src={src}
        autoPlay
        muted
        loop
        playsInline
        style={{
          width: '100%',
          height: 'auto',
          display: 'block'
        }}
      />
    </div>
  );
}

export default Video; 