---
name: Video
---
export function Video({ src }) {
  return (
    <div style={{ 
      width: '100%', 
      margin: '2rem auto',
      marignBottom: 0,
      borderRadius: '4px',
      overflow: 'hidden',
      backgroundColor: "#3E4852"
    }}>
      <video
        src={src}
        autoPlay
        muted
        loop
        playsInline
        style={{
          width: '100%',
          height: 'auto',
          display: 'block'
        }}
      />
    </div>
  );
}

export default Video;