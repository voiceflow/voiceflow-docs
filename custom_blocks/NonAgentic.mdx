---
name: NonAgentic
---
export const NonAgentic = ({ text }) => {
  return (
    <Callout icon="❗️" theme="error">
      <strong style={{marginLeft: "0.85rem"}}>This feature is an older way to build using Voiceflow.</strong><br /><br />
			<p>Although this feature remains available, we don't recommend using it. {text} Legacy features often consume more credits and lead to a worse user experience.</p>
</Callout>

  );
};

<NonAgentic text="Do this instead." />