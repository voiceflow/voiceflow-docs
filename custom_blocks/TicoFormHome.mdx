---
name: TicoFormHome
---
<div className="tico-wrapper">
  <div className="tico-header" style={{ display: 'flex', alignItems: 'center' }}>
    <img
      className="tico-avatar"
      src="https://files.readme.io/8d26c8fc508d0f9b430f56abbf3166b0e2ba373ff6c68ee3e8a6721fe5963dc8-Frame_48095775.png"
      style={{
        width: '3em',
        borderRadius: '50%',
        display: 'inline-block',
      }}
    />
    <div
      className="tico-bubble"
      style={{
        display: 'inline-block',
        marginLeft: '1em',
        backgroundColor: '#53575B',
        padding: '1em',
        borderRadius: '4px',
        width: '100%',
      }}
    >
      <p style={{ marginBottom: '0' }}>Hi, I'm Tico! 👋 Ask me questions about Voiceflow and I'll find the answers for you!</p>
    </div>
  </div>

  <div className="tico-form-wrapper" style={{ marginTop: '1em' }}>
    <form
      className="tico-form"
      onSubmit={(e) => {
        e.preventDefault();

        const input = e.target.elements['tico-message'];
        const message = input?.value?.trim() || null;

        if (message) {
          window.cleanStartTico();
          setTimeout(() => {
            window.searchTico(message);
          }, 1500);
          input.value = '';
        } else if (!message) {
          console.warn('Message is empty');
        } else {
          console.warn('Voiceflow chat is not loaded');
        }

        return false;
      }}
      style={{ display: 'flex', alignItems: 'center', gap: '1em' }}
    >
      <div className="tico-input-wrapper" style={{ position: 'relative', width: '100%' }}>
        <input
          className="tico-input"
          type="text"
          id="tico-message"
          name="tico-message"
          placeholder="Hey Tico! I need help with..."
          style={{
            backgroundColor: '#ECF0F4',
            borderRadius: '6px',
            width: '100%',
            padding: '0.7em 4em 0.7em 1em',
            color: '#202428',
            border: '1px solid #13171B',
            fontSize: '1em',
            height: '3em',
            boxSizing: 'border-box',
          }}
        />
        <button
          className="tico-submit"
          type="submit"
          style={{
            position: 'absolute',
            top: '50%',
            right: '0.5em',
            transform: 'translateY(-50%)',
            height: '2.2em',
            padding: '0 0.75em',
            backgroundColor: '#3554e1',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            fontSize: '0.9em',
            cursor: 'pointer',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          <p style={{ marginBottom: '0' }}>Send</p>
        </button>
      </div>
      <img
        className="tico-avatar-secondary"
        src="https://files.readme.io/fd68b8868d6a64767d905db825614801e75313602a00b7d664618dec2b22c153-Frame_48095776.png"
        style={{
          width: '3em',
          borderRadius: '50%',
          display: 'inline-block',
        }}
      />
    </form>
  </div>
</div>

<TicoFormHome />
