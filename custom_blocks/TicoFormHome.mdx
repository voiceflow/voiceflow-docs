---
name: TicoFormHome
---
<div>
  <div style={{ display: 'flex', alignItems: 'center' }}>
    <img
      src="https://files.readme.io/8d26c8fc508d0f9b430f56abbf3166b0e2ba373ff6c68ee3e8a6721fe5963dc8-Frame_48095775.png"
      style={{
        width: '3em',
        borderRadius: '50%',
        display: 'inline-block',
      }}
    />
    <div
      style={{
        display: 'inline-block',
        marginLeft: '1em',
        backgroundColor: '#53575B',
        padding: '1em',
        borderRadius: '4px',
        width: '100%',
      }}
    >
      <p
        style={{
					marginBottom: '0'
        }}
			>Hi, I'm Tico! 👋 Ask me questions about Voiceflow and I'll find the answers for you!</p>
    </div>
  </div>

  <div style={{ marginTop: '1em' }}>
    <form
      onSubmit={(e) => {
        e.preventDefault();

        const input = e.target.elements['tico-message'];
        const message = input?.value?.trim() || null;

        if (message) {
          window.cleanStartTico();
					setTimeout(() => {
            window.searchTico(message);
          }, 1500);

          /*window.voiceflow.chat.interact({
            type: 'launch',
            payload: { message: message },
          });*/
					
          input.value = ''; // clear input field
        } else if (!message) {
          console.warn('Message is empty');
        } else {
          console.warn('Voiceflow chat is not loaded');
        }

        return false;
      }}
    >
      <input
        type="text"
        id="tico-message"
        name="tico-message"
				placeholder="Hey Tico! I need help with..."
        style={{
          marginRight: '1em',
          backgroundColor: '#ECF0F4',
          borderRadius: '4px',
          width: 'calc(100% - 4em)',
          padding: '0.7em',
          color: '#202428',
          border: '1px solid #13171B',
        }}
      />
      <img
        src="https://files.readme.io/fd68b8868d6a64767d905db825614801e75313602a00b7d664618dec2b22c153-Frame_48095776.png	"
        style={{
          width: '3em',
          borderRadius: '50%',
          display: 'inline-block',
        }}
      />
    </form>
  </div>
</div>
<TicoFormHome />