---
name: RecentReleases
---
import React from 'react';

export function RecentReleases() {
  const releases = [
    {
      icon: 'fa-robot',
      title: 'AI Agent',
      description: 'Edit and review your docs with AI',
      beta: true,
    },
    {
      icon: 'fa-paintbrush',
      title: 'New Themes',
      description: 'New themes and more on the way—with more options',
      beta: false,
    },
    {
      icon: 'fa-server',
      title: 'MCP Servers',
      description: 'Create MCP servers from your docs',
      beta: true,
    },
    {
      icon: 'fa-code-branch',
      title: 'Branches',
      description: 'Save changes without publishing',
      beta: false,
    },
    {
      icon: 'fa-file-alt',
      title: 'LLMs.txt',
      description: 'Make your docs LLM-friendly',
      beta: false,
    },
    {
      icon: 'fa-magnifying-glass',
      title: 'Ask AI',
      description: 'Make it easy for users to leverage AI',
      beta: false,
    },
  ];

  const ReleaseItem = ({ icon, title, description, beta }) => (
    <div className="flex items-start space-x-4 p-2">
      <i className={`fa-solid ${icon} text-gray-400 mt-1`}></i>
      <div>
        <h3 className="font-semibold text-white">
          {title}
          {beta && <span className="ml-2 text-xs font-semibold text-black bg-yellow-400 px-2 py-0.5 rounded-full">Beta</span>}
        </h3>
        <p className="text-gray-400">{description}</p>
      </div>
    </div>
  );

  return (
    <div className="bg-gray-900 p-8 rounded-lg max-w-4xl mx-auto">
      <h2 className="text-2xl font-bold text-white mb-6">Recent Releases</h2>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-x-6 gap-y-4">
        {releases.map((release) => (
          <ReleaseItem key={release.title} {...release} />
        ))}
      </div>
    </div>
  );
}

<RecentReleases />
